import { PddKeywordPipe } from './pdd-keyword.pipe';

describe('PddKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new PddKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
